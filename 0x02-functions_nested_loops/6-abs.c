#include "main.h"

/**
 *  _abs - name of function
 *  Description: calculate the absolute value
 *  @w: convert number
 *  Return: 0
 */

int _abs(int w)
{
	if (w < 0)
		return (w * -1);
	else
		return (w);
}
